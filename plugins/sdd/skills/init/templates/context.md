<!--
  feature-spec/context.md — AI-readable project context.

  Purpose: tell AI (and humans) WHERE to make changes and WHICH rules cannot be broken.
  Persistence: long-lived structural facts. Auto-generated by /init, kept in sync by /complete.
  Boundary:
    - feature-spec/context.md    → durable structure (this file): layers, domain map, entry points, hard rules
    - feature-spec/knowledge.md  → accumulated gotchas: cross-file coupling, non-obvious traps, migration baggage
    - feature-spec/config.yaml   → tool-runnable config: lint commands, verification commands

  Editing rules (apply to every section):
    1. Versions appear ONCE in Tech Stack & Versions. Do not inline a version number anywhere
       else (Mission, Hard Rules, etc.) — say "current major" or just the framework name.
    2. Use **major version only** in Tech Stack (e.g. `Nuxt 3`, not `Nuxt 3.17.6`). Rely on
       package.json / *.csproj / pyproject.toml for the exact patch — otherwise this doc rots
       on every minor/patch bump. Exception: explicit `5.x` / `>= 1.3` style ranges are fine.
    3. Use pointer form, not enumeration. Prefer "N items under `path/`" over listing every
       file/component. Do NOT mix the two — neither "incl. X / Y" nor parenthetical lists like
       "8 subdirs in this path (banners, games, icon, ...)" or "9 in pages/[[locale]]/
       (Content.vue, BDContent.vue, ...)". The count makes the list redundant; the list makes
       the count rot. Pick one — pointer is preferred.
    4. Hard Rules are STRUCTURAL invariants (true a year from now). Migration leftovers,
       "do not use the old X" notes, and version-pinned warnings belong in knowledge.md/Gotchas.
    5. If a rule is enforced by ESLint / dotnet-format / Ruff / lint config, it does NOT belong
       in Hard Rules — point to the config file in Cross-cutting Concerns instead. Hard Rules
       are for invariants linters cannot express (layer boundaries, allowed call directions).

  Editing this file by hand is fine — /complete merges new facts without overwriting your edits.
  Drop optional sections (Conditional Subsystems, Anti-patterns, Glossary) entirely if not needed —
  do not leave empty stubs.
-->

# Project Context

<!-- Auto-generated by /init on YYYY-MM-DD -->

## Mission

<One or two sentences: what this project does and who it serves. NO version numbers here.>

## Tech Stack & Versions

<Single source of truth for versions. Pull values from manifest files; do not duplicate elsewhere.>

- Language: <e.g., TypeScript 5.x / C# 13 — from tsconfig.json / *.csproj>
- Framework: <e.g., Nuxt 4, ASP.NET Core 10>
- Build: <e.g., Vite 6, MSBuild>
- Package manager: <pnpm / npm / yarn / NuGet>

## Architecture Layers

<Name each layer, what it owns, and the allowed call direction. Tells AI which layer to put new logic in.>

```
<e.g.>
Domain ← Application ← Infrastructure ← Api
- Domain         : pure business logic, no framework dependencies
- Application    : use case orchestration, DTO mapping
- Infrastructure : DB, external API, file IO implementations
- Api            : HTTP entry, request/response only — no business logic
```

## Domain-to-Code Map

<Map each business domain to its primary folder. Use pointer form, NOT enumeration. The reader follows the path to discover what's there.>

| Domain | Path | Notes |
|---|---|---|
| <e.g., User auth> | `src/Auth/` | JWT + refresh token; ~6 items under this path |
| <e.g., Order management> | `src/Orders/` | aggregate root: `Order`; N items under `src/Orders/` |

<!--
  ❌ BAD: enumerating every component (rots whenever a file is added or renamed)
       | Order | src/Orders/ | OrderService, OrderRepository, OrderValidator, OrderController, OrderDto, ... |
  ✅ GOOD: pointer + cardinality + key fact
       | Order | src/Orders/ | aggregate root: `Order`; N items under this path |
-->

## Entry Points

<Where execution starts. Tells AI where to add a new endpoint, page, job, or handler.>

<!--
  AI generation checklist — scan ALL conventional locations and list each one that exists.
  Missing one entry point silently breaks AI's ability to add features in that area.

    HTTP API:           src/**/Endpoints/, src/**/Controllers/, server/api/, server/routes/
    Frontend pages:     pages/**/*.vue, app/pages/, app/routes/, src/views/
    Middleware:         middleware/, server/middleware/
    Plugins / modules:  plugins/, modules/, src/Modules/
    Background jobs:    src/**/Jobs/, src/**/Workers/, worker/
    Event handlers:     src/**/EventHandlers/, src/**/Handlers/
    CLI / scripts:      bin/, scripts/, src/Cli/
-->

- HTTP API: <e.g., `src/Api/Endpoints/` — Minimal API style>
- Frontend pages: <e.g., `pages/*.vue` — Nuxt file-based routing>
- Server middleware: <e.g., `server/middleware/`>
- Plugins / modules: <e.g., `plugins/`, `modules/`>
- Background jobs: <e.g., `src/Worker/Jobs/`>
- Domain event handlers: <e.g., `src/Application/EventHandlers/`>

## Conditional Subsystems

<Optional. Use when a subsystem activates only under specific conditions (request header, feature flag, route prefix, environment). DROP THIS SECTION ENTIRELY if no such subsystems exist — do not leave an empty stub.>

<!--
  Each entry MUST state the trigger condition, not just the behavior.
  AI cannot route logic correctly without knowing when the subsystem activates.
-->

- **<Subsystem>**: <one-line behavior>
  - **Trigger**: <exact condition — e.g., "request has `X-Render-Props` header", "`features.foo` flag enabled", "path starts with `/admin/`">
  - **Code path**: <where the branching happens>
  - e.g., **Island mode**: server-side renders a fragment instead of a full page.
    Trigger: request has `X-Render-Props` header. Code path: `server/middleware/island.ts`.

## Cross-cutting Concerns

<Where shared mechanisms live. Stops AI from re-implementing things that already exist.>

<!--
  Keep this section lean. If you have more than ~5 high-frequency concerns, split the
  high-traffic ones into their own H2 sections (## Caching, ## Internationalization,
  ## Datetime) and leave only the long tail here.
-->

- Error handling: <e.g., `src/Api/Middlewares/GlobalExceptionMiddleware.cs` — throw `DomainException`, do not write try/catch in endpoints>
- Authentication: <e.g., `[Authorize]` + policies in `src/Api/Auth/Policies/`>
- Logging: <e.g., Serilog, configured in `Program.cs`; use `ILogger<T>`>
- Caching: <e.g., HybridCache with `IHybridCache`>
- Frontend data fetching: <e.g., `composables/useApi.ts` — wraps fetch + error toast>

## Hard Rules

<STRUCTURAL invariants only. One short imperative line each. Violations rejected at PR review.>

<!--
  ✅ Belongs here (structural — true a year from now):
     - Endpoints must not access DbContext directly — go through Application layer
     - Vue components must not call fetch directly — use useApi
     - No business logic in Infrastructure/ — only IO

  ❌ Does NOT belong here (move to knowledge.md / Gotchas):
     - "Toolkit subpath imports were removed in 1.3.0" — version-pinned migration leftover
     - "Do not use the old AuthService" — historical, will rot once old code is gone
     - "After upgrading X, do Y" — operational recipe, not an invariant

  Smell test: if the rule could become wrong after a refactor or version bump,
  it is historical, not structural — push it to knowledge.md.
-->

- <e.g., Endpoints must not access `DbContext` directly — go through Application layer>
- <e.g., Vue components must not call `fetch` directly — use `useApi`>
- <e.g., No business logic in `Infrastructure/` — only IO>

## Anti-patterns

<Optional. Common mistakes plus WHY they fail. Drop this section entirely if no concrete examples yet — do not leave an empty stub.>

<!--
  Difference vs Hard Rules:
    Hard Rules     = "must not X" (the rule).
    Anti-patterns  = "people often write X expecting Y, but it breaks Z" (the lesson + the why).
  Useful for AI code review — searchable as a single block.
-->

- <e.g., **Calling `useApi` inside `computed`** — `useApi` runs on setup; calling it inside `computed` re-runs every dep change and floods the network. Use `await` in setup or `useAsyncData` instead.>

## Glossary

<Optional. Drop the entire ## Glossary section if the domain is simple CRUD. Otherwise split into the two subsections below.>

### Code Terms

<Names that exist in code (folders, classes, components). Auto-syncable by /complete when new domain folders appear.>

- **<Term>**: <one-line definition + where it lives>
  - e.g., **Section**: a Lego-style content block on the homepage. `components/section/Section*.vue`.

### Business Terms

<Domain abbreviations and concepts NOT visible in code. Human-curated; /complete will not touch this.>

- **<Term>**: <Definition + boundary vs adjacent terms>
  - e.g., **PPC**: pay-per-click affiliate channel. Distinct from `Affiliate` (the partner) and `Click` (the event).

## Common Commands

<Most-used dev commands. Pull from package.json scripts / Makefile / project README.>

- Dev server: <e.g., `pnpm dev`>
- Type check: <e.g., `pnpm type-check`>
- Unit tests: <e.g., `pnpm test:unit`>
- E2E tests: <e.g., `pnpm test:e2e`>
- Build: <e.g., `pnpm build`>
- Lint + fix: <e.g., `pnpm lint --fix`>
